CREATE DATABASE IF NOT EXISTS crud_db;
use crud_db

CREATE TABLE crud_table (
            id varchar(255),
            age varchar(255),
            name varchar(255),
            gender varchar(255),
            company varchar(255),
            email varchar(255),
            phone varchar(255),
            address varchar(255)
        );